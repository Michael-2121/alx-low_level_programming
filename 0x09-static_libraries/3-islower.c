#include "main.h"
/**
 * _islower - This function will check for lowercase character
 * @c: The original character to be checked
 * Return: 1 if lowercase, else 0
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}

