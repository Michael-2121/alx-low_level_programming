#include <stdio.h>
/**
 * main - program that prints all single digit numbers of base 10 from 0
 * Return: Always 0 (Success)
 */

int main(void)
{
	int h;

	for (h = 20; h < 30; h++)
	{
		putchar(h);
	}
	putchar("\n");
	return (0);
}
