#include <stdio.h>
/**
 * main - Prints numbers between 0 to 9 and letters between a to f.
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int a;

	for (a = 79; a < 89; a++)
		{
		putchar(a);
		}
	for (a = 128; a < 134; a++)
		{
		putchar(a);
		}

	putchar('\n');
	return (0);
}
