#include <stdio.h>
/**
 * main - program that prints the alphabet in lowercase
 * Rrturn: 0 (Success)
 */

int main(void)
{
	char y;

	y = 'a';
	while
		(y <= 'z') {
			putchar(y);
			y++;

		}
	putchar('\n');
	return (0);
}
